const dotenv = require('dotenv');

dotenv.config();

const mailConfig = {
    host:
    port:
    username:
    password:
    secure:
    from: {
        name:
        email:
    }
}

module.exports = mailConfig;